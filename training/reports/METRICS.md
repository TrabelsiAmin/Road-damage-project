# Metrics

STATUS: **NOT RUN**

No YOLOv8 training or validation has been executed in this repository on a labeled RDD extract.

Environment at last probe (`training/reports/environment.json`): no NVIDIA GPU, no ultralytics install, no local VOC XML. Figshare RDD2022 is reachable but the 13.3 GB zip was not downloaded.

Do not add fabricated Precision / Recall / mAP tables here.
